# Disposable repro workflow. Delete after validation.

name: "PR 8670 deterministic A/B - Linux"

on:
  push:
    branches:
      - "fix/pr-8670-backend-repair-ab-*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repro:
    name: "Targeted repro"
    runs-on: "ubuntu-latest"
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405
        with:
          python-version: "3.12"

      - name: "Run deterministic Linux repair contract"
        shell: bash
        run: |
          bash .github/repro/pr8670_linux_probe.sh

